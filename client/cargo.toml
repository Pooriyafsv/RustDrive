[package]
name = "client"
version = "0.1.0"
edition = "2024"

[dependencies]
common = {path = "../common"}

tokio = {version = "1.28.0", features = ["full"]}

serde = {version= "1",features= ["derive"]}
serde_json = "1"

thiserror = "2"

log = "0.4.0"
env_logger = "0.11"

indicatif = "0.18"